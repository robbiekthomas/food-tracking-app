import React from 'react';
import classNames from 'classnames';

const ChartsHeader = ({ title }) => {
 
   return(<p className="text-white opacity-50 mb-1 opacity-50 font-sans font-semibold leading-normal text-l" >{title}</p>) 
};

export default ChartsHeader;